[package]
name = "typing-core"
description = "Touch-typing engine ported from Klavaro: keyboard layouts, lessons, exercise generation and scoring. Pure logic, no I/O."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints.rust]
missing_docs = "warn"
