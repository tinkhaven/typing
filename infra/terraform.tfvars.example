# Copy to terraform.tfvars and adjust. Defaults suit typing.tinkhaven.com.

# aws_region        = "eu-west-1"
# domain_name       = "typing.tinkhaven.com"
# route53_zone_name = "tinkhaven.com"

# X86_64 if you build images on an Intel machine.
# cpu_architecture = "ARM64"

# See the variable's comment before raising this above 1.
# desired_count = 1
