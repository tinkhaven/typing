.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# Saved plans embed the full resolved configuration, account identifiers
# included. Never commit one.
tfplan
*.tfplan
*.tfplan.*
